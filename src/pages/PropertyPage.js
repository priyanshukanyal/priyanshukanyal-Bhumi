import React from "react";
import PropertyForm from "../components/PropertyForm";

const PropertyPage = () => {
  return (
    <div>
      <PropertyForm />
    </div>
  );
};

export default PropertyPage;
